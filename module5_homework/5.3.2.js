let x = true;

if (typeof x === 'string') {alert('это строка')}

else {
    if (typeof x === 'boolean') {alert('это лог.тип')}
    else { if (typeof x === 'number') {alert ('это число')}
    else {alert('тип не определен')}

    }
}

